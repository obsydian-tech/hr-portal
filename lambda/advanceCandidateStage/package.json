{
  "name": "advance-candidate-stage",
  "version": "1.0.0",
  "description": "TalentFlow advanceCandidateStage Lambda — forward-only stage transition with EventBridge audit",
  "main": "index.js",
  "scripts": {
    "test": "jest --no-coverage"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "jest": "^29.0.0"
  }
}
