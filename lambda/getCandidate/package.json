{
  "name": "get-candidate-lambda",
  "version": "1.0.0",
  "description": "TalentFlow getCandidate Lambda — GetItem by PK/SK",
  "private": true,
  "scripts": {
    "test": "jest --coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
