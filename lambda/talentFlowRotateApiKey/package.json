{
  "name": "talent-flow-rotate-api-key",
  "version": "1.0.0",
  "description": "90-day Agent API key rotation cron Lambda",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "node --experimental-vm-modules node_modules/.bin/jest --no-coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/?(*.)+(spec|test).[jt]s?(x)",
      "**/?(*.)+(spec|test).mjs"
    ],
    "transform": {}
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@jest/globals": "^29.0.0",
    "jest": "^29.0.0"
  }
}
