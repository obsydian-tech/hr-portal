{
  "name": "talent-flow-monitor-slas",
  "version": "1.0.0",
  "description": "Hourly SLA monitor — scans open SAGA records, publishes SLABreached events",
  "main": "index.js",
  "scripts": {
    "test": "npx jest --no-coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "clearMocks": false
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/client-eventbridge": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
