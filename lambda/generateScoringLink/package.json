{
  "name": "generate-scoring-link",
  "version": "1.0.0",
  "description": "TalentFlow generateScoringLink Lambda — creates token-gated scoring links for email-only panelists",
  "main": "index.js",
  "scripts": {
    "test": "jest --no-coverage"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
