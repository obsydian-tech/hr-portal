{
  "name": "admin-get-users-lambda",
  "version": "1.0.0",
  "description": "TalentFlow adminGetUsers Lambda — admin user registry query",
  "private": true,
  "scripts": {
    "test": "jest --no-coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
