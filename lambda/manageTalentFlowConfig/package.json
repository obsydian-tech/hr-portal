{
  "name": "manage-talent-flow-config",
  "version": "1.0.0",
  "description": "TalentFlow config management Lambda — versioned config CRUD, admin-only writes",
  "main": "index.js",
  "scripts": {
    "test": "jest --no-coverage"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
