{
  "name": "admin-get-dashboard-lambda",
  "version": "1.0.0",
  "description": "TalentFlow adminGetDashboard Lambda — admin Global Dashboard KPI aggregation",
  "private": true,
  "scripts": {
    "test": "jest --no-coverage"
  },
  "dependencies": {},
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/util-dynamodb": "^3.0.0",
    "jest": "^29.0.0"
  }
}
